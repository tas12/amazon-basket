import React from 'react';
import ReactDOM from 'react-dom';
import Basket from './basket-components/basket.js'

const container = document.getElementById('container');

ReactDOM.render(<Basket />, container);
